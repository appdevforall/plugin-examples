<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="AI Core Plugin"
        android:theme="@style/PluginTheme">

        <!-- Plugin metadata -->
        <meta-data
            android:name="plugin.id"
            android:value="com.itsaky.androidide.plugins.aicore" />

        <meta-data
            android:name="plugin.name"
            android:value="AI Core" />

        <meta-data
            android:name="plugin.version"
            android:value="${pluginVersion}" />

        <meta-data
            android:name="plugin.description"
            android:value="The Agent chat and the LLM inference router. Install it plus at least one backend plugin (AI Agent Local, AI Agent Gemini) to use any AI feature." />

        <meta-data
            android:name="plugin.author"
            android:value="App Dev for All" />

        <meta-data
            android:name="plugin.min_ide_version"
            android:value="26.33" />

        <meta-data
            android:name="plugin.max_ide_version"
            android:value="26.99" />

        <!-- Number of editor tabs contributed via getEditorTabs() (the "Agent" tab) -->
        <meta-data
            android:name="plugin.editor_tabs"
            android:value="1" />

        <!-- The agent's tools read and write project files, run Gradle and inspect project
             structure. No network.access: the backend plugins make every outbound call. -->
        <meta-data
            android:name="plugin.permissions"
            android:value="filesystem.read,filesystem.write,system.commands,project.structure" />

        <meta-data
            android:name="plugin.main_class"
            android:value="com.itsaky.androidide.plugins.aicore.plugin.AiCorePlugin" />

        <meta-data
            android:name="plugin.icon_day"
            android:value="assets/icon_day.png" />

        <meta-data
            android:name="plugin.icon_night"
            android:value="assets/icon_night.png" />

    </application>

</manifest>
