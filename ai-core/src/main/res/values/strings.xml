<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Intentionally empty. AI Core has no user-facing strings of its own: every message a user
      reads comes from the backend plugin that produced it (ai-backend-gemini's gemini_error_*,
      ai-backend-local's llm_load_error_*), resolved against that plugin's own resources.
    -->
</resources>
