<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp" />
    <!--
        Translucent material-green; the host theme provides colorTertiary
        on top in dark mode so the green still reads against the dark
        surface. Hardcoded fallback for theme lookups that fail (e.g.
        layout preview).
    -->
    <solid android:color="#334CAF50" />
</shape>
