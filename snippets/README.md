# Snippets

A placeholder. `.cgs` snippet files will live here later.

Nothing here is a plugin. The snippet *plugin* — the one that manages snippets
inside the editor — is `plugins/Favorite-Snippets/`; this directory held it
before that move and keeps the name only for the `.cgs` files to come.
