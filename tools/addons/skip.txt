# One directory name for each line. State the reason after the name.
#
# NOTE: skipping an addon stops it building, being packaged, and reaching the
# catalog. PRD acceptance criterion 13 says the six AI addons "still build and
# publish unchanged", which this list does NOT satisfy. They are here because
# the owner asked that they not be touched while other pull requests are in
# flight, and publishing them needs an addon.json and a rename. That is a
# deliberate, recorded gap, not an oversight -- see O10 in the design.
pebble-custom-function-template-installer  ships stale copies of the shared jars
cotg-ndk  held until the contributor confirms the name and the credit
ai-agent-gemini  other pull requests in flight; do not touch the ai addons yet
ai-agent-local  other pull requests in flight; do not touch the ai addons yet
ai-agent-mcp  other pull requests in flight; do not touch the ai addons yet
ai-agent-openai  other pull requests in flight; do not touch the ai addons yet
ai-core  other pull requests in flight; do not touch the ai addons yet
ai-literacy-course  other pull requests in flight; do not touch the ai addons yet
